-- Table: public.studentdetails

-- DROP TABLE IF EXISTS public.studentdetails;

CREATE TABLE IF NOT EXISTS public.studentdetails
(
    name character varying(50) COLLATE pg_catalog."default",
    phone character varying(10) COLLATE pg_catalog."default",
    regno numeric
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.studentdetails
    OWNER to postgres;
